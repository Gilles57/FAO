name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: ls -a via ssh
      uses: matheusvanzan/sshpass-action@v2
      with:
          host: alligator.o2switch.net
          user:  gsalmon
          pass: vWZzt4uKvchabkT0
          run: |
            ls -a
