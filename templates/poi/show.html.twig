{% extends 'base.html.twig' %}

{% block title %}P.O.I.{% endblock %}

{% block body %}
    <h1>P.O.I.</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poi.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ poi.name }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ poi.lat }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ poi.lon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('poi_index') }}">Retour à la liste</a>

    <a href="{{ path('poi_edit', {'id': poi.id}) }}">Modifier</a>

    {{ include('poi/_delete_form.html.twig') }}
{% endblock %}
