{% extends 'base.html.twig' %}

{% block title %}Ajouter un commentaire - FAO TRAVEL !{% endblock %}
{% block body %}
    <div class="row justify-content-center">

        <div class="col-md-6 bg-light p-2">
            <h1 class="h2 mb-4 text-center">Ajouter un commentaire</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
{#            {{ form_row(form.recaptcha) }}#}
{#            {{ form_errors(form.recaptcha) }}#}
            <button type="submit" class="btn btn-secondary w-100" formnovalidate>Envoyer</button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
