{% extends 'base.html.twig' %}
{# TODO RGPD #}
{% block title %}Plan du site - Fao Travel{% endblock %}
{% block body %}
    <div class="container">
        {#        {{ dump(urls) }} #}
        <h1 class="bg-secondary text-center">Plan du site</h1>
        {% if is_mobile() %}
            <div class="row p-1">

            </div>
        {% else %}
            <div class="row">

            </div>
        {% endif %}
        <div class="justify-content-around d-flex">
            <div class="illustration p-2">
                <h2 class="h5">Pages principales</h2>
                <hr>
                <ul>
                    {% for url in urls %}
                        {% if url.partie == 'Principale' %}
                            <li><a href="{{ url.loc }}" class="text-decoration-none">{{ url.titre }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
            <div class="illustration p-2">
                <h2 class="h5">Évenements</h2>
                <hr>
                <ul>
                    {% for url in urls %}
                        {% if url.partie == 'Event' %}
                            <li><a href="{{ url.loc }}" class="text-decoration-none">{{ url.titre }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
            <div class="illustration p-2">
                <h2 class="h5">Souvenirs</h2>
                <hr>
                <ul>
                    {% for url in urls %}
                        {% if url.partie == 'Souvenir' %}
                            <li><a href="{{ url.loc }}" class="text-decoration-none">{{ url.titre }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
            <div class="illustration p-2">
                <h2 class="h5">Livre d'or</h2>
                <hr>
                <ul>
                    {% for url in urls %}
                        {% if url.partie == 'Livre' %}
                            <li><a href="{{ url.loc }}" class="text-decoration-none">{{ url.titre }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
            <div class="illustration p-2">
                <h2 class="h5">Partenaires</h2>
                <hr>
                <ul>
                    {% for url in urls %}
                        {% if url.partie == 'Partenaire' %}
                            <li><a href="{{ url.loc }}" class="text-decoration-none">{{ url.titre }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
            <div class="illustration p-2">
                <h2 class="h5">Formulaires</h2>
                <hr>
                <ul>
                    {% for url in urls %}
                        {% if url.partie == 'Formulaire' %}
                            <li><a href="{{ url.loc }}" class="text-decoration-none">{{ url.titre }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>

        </div>
    </div>
{% endblock %}

