{% extends 'base.html.twig' %}

{% block title %}Article du {{ article.publishedAt | format_date ('short', locale='fr') }} - FAO TRAVEL !{% endblock %}
{% block body %}
    <div class="d-flex justify-content-between bg-dark text-secondary">
        <div class="p-2">{{ article.reference }}</div>
        <div class="p-2">{{ article.publishedAt | format_date ('full', locale='fr') }}</div>
    </div>
    <img src="/{{ article.media }}" style="width: 100%; height: 100%;" alt="Article de {{ article.reference }} publié le {{ article.publishedAt | format_date ('short', locale='fr') }}">
{% endblock %}
