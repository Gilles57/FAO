{% extends 'base.html.twig' %}

{% block title %}Nouvel événement{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-xl-6 m-auto">
            <h1>Créer un nouvel événement</h1>
            <div class="p-3 bg-secondary">
                {{ include('_partials/_form.html.twig') }}
            </div>

            <a href="{{ path('app_new_event') }}" class="btn btn-success mt-3">Retour à la liste</a>
        </div>
    </div>
{% endblock %}


{% block javascripts %}
    {{ parent() }}
{% endblock %}
